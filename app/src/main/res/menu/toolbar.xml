<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".activity.MainActivity">

    <item
        android:id="@+id/overflow_export_responses"
        android:title="@string/overflow_export_responses"
        app:showAsAction="never"/>

    <item
        android:id="@+id/overflow_export_scores"
        android:title="@string/overflow_export_scores"
        app:showAsAction="never"/>

    <item
        android:id="@+id/overflow_import_responses"
        android:title="@string/overflow_import_responses"
        app:showAsAction="never"/>

    <item
        android:id="@+id/overflow_import_scores"
        android:title="@string/overflow_import_scores"
        app:showAsAction="never"/>

    <item
        android:id="@+id/overflow_information"
        android:title="Information"
        app:showAsAction="never"/>

    <item
        android:id="@+id/overflow_settings"
        android:title="@string/overflow_settings"
        app:showAsAction="never"/>

</menu>